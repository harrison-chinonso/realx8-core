{
  "$schema": "https://railway.app/railway.schema.json",
  "_comment": "For the FULLY SPLIT shape only. Keep the Railway service's root directory at the repo root — dependencies and shared/ resolve from there. The simpler split is one Railway service per group running 'npm start' with SERVICES set; see DEPLOYMENT.md.",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "node services/user-service/src/index.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
